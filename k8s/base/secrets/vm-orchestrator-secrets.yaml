apiVersion: v1
kind: Secret
metadata:
  name: vm-orchestrator-secrets
  namespace: vcenter-provisioner
type: Opaque
data:
  # No sensitive data needed for vm-orchestrator
  # Uses auth-service and other services via URLs
