apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: credential-manager-hpa
  namespace: vcenter-provisioner
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: credential-manager
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
