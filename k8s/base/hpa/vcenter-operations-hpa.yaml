apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vcenter-operations-hpa
  namespace: vcenter-provisioner
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vcenter-operations
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
