apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vcenter-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: vcenter-ca
  secretName: vcenter-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
