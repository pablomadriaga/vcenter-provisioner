apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vcenter-ca-issuer
spec:
  ca:
    # Secret generated by certificate-ca.yaml in namespace cert-manager
    secretName: vcenter-ca-secret
