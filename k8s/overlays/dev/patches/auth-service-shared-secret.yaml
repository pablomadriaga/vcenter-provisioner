apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  template:
    spec:
      containers:
      - name: auth-service
        envFrom:
        - secretRef:
            name: shared-secrets
        - configMapRef:
            name: auth-service-config
        - secretRef:
            name: auth-service-secrets
