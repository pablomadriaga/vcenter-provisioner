apiVersion: v1
kind: Service
metadata:
  name: typing-service
  namespace: default
spec:
  selector:
    app: typing-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
