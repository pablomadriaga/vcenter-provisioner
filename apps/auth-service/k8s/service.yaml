apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: vcenter-provisioner
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: 3001
      targetPort: 3001
      name: http
