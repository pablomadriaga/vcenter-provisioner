apiVersion: v1
kind: Service
metadata:
  name: stats-service
  namespace: default
spec:
  selector:
    app: stats-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
