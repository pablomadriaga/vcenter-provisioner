apiVersion: v1
kind: Service
metadata:
  name: vm-orchestrator
  namespace: vcenter-provision-er
  labels:
    app: vm-orchestrator
spec:
  type: ClusterIP
  selector:
    app: vm-orchestrator
  ports:
    - port: 8080
      targetPort: 8080
      name: http
