apiVersion: v1
kind: Service
metadata:
  name: vm-orchestrator
  namespace: default
spec:
  selector:
    app: vm-orchestrator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
