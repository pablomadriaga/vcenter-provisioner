apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: default
spec:
  selector:
    app: monitoring-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
