apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: vcenter-provisioner
  labels:
    app: monitoring-service
spec:
  type: ClusterIP
  selector:
    app: monitoring-service
  ports:
    - port: 8082
      targetPort: 8082
      name: http
