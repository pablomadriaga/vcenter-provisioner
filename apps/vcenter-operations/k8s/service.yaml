apiVersion: v1
kind: Service
metadata:
  name: vcenter-operations
  namespace: vcenter-provisioner
  labels:
    app: vcenter-operations
spec:
  type: ClusterIP
  selector:
    app: vcenter-operations
  ports:
    - port: 8091
      targetPort: 8091
      name: http
