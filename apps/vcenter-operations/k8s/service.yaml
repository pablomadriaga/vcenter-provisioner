apiVersion: v1
kind: Service
metadata:
  name: vcenter-integration
  namespace: default
spec:
  selector:
    app: vcenter-integration
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
